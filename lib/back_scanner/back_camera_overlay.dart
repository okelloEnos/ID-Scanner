import 'package:flutter/material.dart';

/// for overlaying functionality like drawing a rectangle on top of the livestream
class BackSideCameraOverlay extends StatelessWidget {
  const BackSideCameraOverlay({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
